# Burgerreich-watch runtime dependencies.
#
# watchcore is the shared engine (Observation contract + store/dedup/query
# stages). This git+https line is for CI. For LOCAL development install the
# sibling checkout editable instead, so watchcore edits are importable without
# pushing:
#
#     pip install -e "../watchcore[dedup]"
#
watchcore[dedup,store] @ git+https://github.com/CSU-J3/watchcore

# Collector runtime deps.
curl_cffi>=0.7.0
feedparser>=6.0.10
